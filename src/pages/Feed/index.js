import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Feed = () => {
    return (
        <View>
            <Text>Feed Page</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Feed;