import React from 'react';
import { View, Image, Text, StyleSheet } from 'react-native';
import { Icons } from '../../components';

const IconsFeed = ({icons}) => {
    return(
        <Icons>{icons}</Icons>
    );
};

export default IconsFeed;
