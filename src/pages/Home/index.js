import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Home = () => {
    return (
        <View>
            <Text>Ini Home</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Home;
